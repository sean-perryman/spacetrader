%h1 Player Ships
%table.table.table-striped
  %tr
    %th Player
    %th Ship Name
    %th Cargo Capacity
    %th{:colspan => "3"}
  - @player_ships.each do |player_ship|
    %tr
      %td= player_ship.player.Name
      %td= player_ship.ship.Name
      %td= player_ship.cargo_total
      %td= link_to 'Show', player_ship, :class => "btn btn-default"
      %td= link_to 'Edit', edit_player_ship_path(player_ship), :class => "btn btn-default"
      %td= link_to 'Destroy', player_ship, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger"
